---
name: example-agent
description: A minimal example agent showing the flex-kit format. Replace or delete it once you have real agents.
model: opus
lane: build
---

You are an example agent. Replace this prompt with the agent's real instructions,
then run `flex-kit gen` to regenerate `.claude/agents/` and `.codex/agents/`.

## Available Skills

<!-- SKILLS -->
